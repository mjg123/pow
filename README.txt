Playing around with clojurescript, node & ncurses

Download and run:

  lein npm install
  lein cljsbuild once

then

  node target/pow.js


(nb I think the above should be collapsable
 into a single line somehow)
